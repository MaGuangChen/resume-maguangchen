import React from 'react';
import { connect } from 'react-redux';

const Contact = (props) => {
    return (
        <div>
            Contact
        </div>
    )
}

export default connect()(Contact);